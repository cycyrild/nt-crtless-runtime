# nt-crtless-runtime

A minimal runtime layer for building small Windows binaries without linking the standard CRT.

Useful for low-level Windows tooling, Native API projects ...

Targets an LLVM/Clang + xwin toolchain.

